# Noindex inventory

A record of every URL on `intobadminton.com` that intentionally emits
`<meta name="robots" content="noindex">`. Maintained so that Google Search
Console alerts about "Excluded by 'noindex' tag" can be triaged quickly
against this list — if the affected URL is in this file, the exclusion is
intentional and the GSC "Validate fix" / "Mark as expected" action is the
correct response.

Last audited: **2026-05-18** — 75 noindex URLs total, 0 of them in the
sitemap (no mixed signals).

## 1. Legacy locale redirects (66 URLs)

Generated by `scripts/generate-legacy-redirects.mjs` from
`src/data/legacy-redirect-destinations.json`. Each redirect page emits
`noindex, follow` plus a `<meta http-equiv="refresh">` to the canonical
unlocalized URL plus a `<link rel="canonical">` to the same destination.

- `/en/` and 32 child paths (e.g. `/en/about/`, `/en/blog/`,
  `/en/guides/string-tension/`)
- `/zh/` and 32 child paths (Chinese-language URL deprecation)

**Rationale.** The site was originally bilingual with `/en/` + `/zh/`
prefixed routes. After the locale system was simplified to English-only,
these URLs were preserved as proper `noindex + canonical + meta-refresh`
redirects so Google's index can transition cleanly. Expected to remain in
place for ~90 days after the locale simplification shipped, then can be
removed once GSC reports them as fully de-indexed.

## 2. Operational / user-state pages (7 URLs)

These render per-device or per-session state, or are pure form / utility
pages with no useful content for a crawler.

| URL | Why noindex |
|---|---|
| `/results/` | Per-user finder output, computed from the query string / saved profile |
| `/setup/` | Operational page, not a landing surface |
| `/compare/` | Empty compare-tray surface (state lives in `localStorage`) |
| `/review/submit/` | Submission form |
| `/privacy-choices/` | Operational consent surface |
| `/blogs/` | Legacy path; redirects to `/blog/` |
| `/saved/` | Per-device shortlist (depends on `localStorage`) |

## 3. Framework / 404 pages (2 URLs)

| URL | Why noindex |
|---|---|
| `/404` | The 404 page itself |
| `/_not-found` | Next.js framework fallback |

## What to do when GSC sends a "noindex" exclusion alert

1. Open the GSC alert and note the affected URL(s).
2. Cross-check against the list above. If the URL falls into one of the
   three categories, the exclusion is **intentional** — click "Validate
   fix" or dismiss the alert.
3. If the URL is **not** in this list, the noindex is unintentional. Check
   for an accidental `robots: { index: false }` in the page's
   `generateMetadata` fallback branch (the dynamic `[slug]` routes have
   such a fallback for unknown slugs — `generateStaticParams` should
   prevent it from ever firing at build time, but a stale slug in
   `blogSlugs[]` without a corresponding article definition would expose
   it).

## Where the noindex declarations live

| File | What it controls |
|---|---|
| `src/app/setup/page.tsx` | `/setup/` |
| `src/app/results/page.tsx` | `/results/` |
| `src/app/compare/layout.tsx` | `/compare/` |
| `src/app/privacy-choices/layout.tsx` | `/privacy-choices/` |
| `src/app/blogs/page.tsx` | `/blogs/` |
| `src/app/review/submit/page.tsx` | `/review/submit/` |
| `src/app/saved/page.tsx` | `/saved/` |
| `src/app/not-found.tsx` | `/_not-found/` and `/404` |
| `src/app/blog/[slug]/page.tsx` | Fallback for unknown blog slugs (should never trigger) |
| `src/app/review/[slug]/page.tsx` | Fallback for unknown product review slugs (should never trigger) |
| `scripts/generate-legacy-redirects.mjs` | All 66 `/en/*` + `/zh/*` redirect pages |

## Cross-checks performed (2026-05-18)

- `find out -name "index.html" -exec grep -l 'noindex' {} \;` → 75 files
- Every noindex page is **absent** from `out/sitemap.xml` (164 sitemap URLs, 0 overlap).
- `out/robots.txt` contains no `Disallow:` directives — robots.txt and noindex meta tags are not in conflict.
